namespace NEXUSDataLayerScaffold.Tests;

// Placeholder file to keep the test project compile-only in environments without restored test packages.
public static class Dummy
{
    public static string Hello => "Tests are disabled for CI-lite build";
}
