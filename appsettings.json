{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Auth0": {
    "ApiIdentifier": "${AUTH0_API_IDENTIFIER}",
    "ClientId": "${AUTH0_CLIENT_ID}",
    "Secret": "${AUTH0_CLIENT_SECRET}",
    "Domain": "${AUTH0_DOMAIN}",
    "Audience": "${AUTH0_AUDIENCE}",
    "UserInfoEndpoint": "${AUTH0_USERINFO_ENDPOINT}"
  }
}
